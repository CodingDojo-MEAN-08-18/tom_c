<!DOCTYPE html>
<html lang='en'>
    <head>
        <meta charset='UTF-8'>
        <title>Quoting Dojo</title>
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css">
    </head>
    <body class='container'>
        <h1>Welcome to Quoting Dojo</h1>
        <form action='/quotes' method='post'>
            <table>
                <tr>
                    <td>Your Name:</td>
                    <td><input type='text' name='name'></td>
                </tr>
                <tr>
                    <td>Your Quote: </td>
                    <td><textarea name='quote' rows='5' cols='80'></textarea></td>
                </tr>
            </table>
            <input type='submit' class="btn btn-primary" value='Add my quote!'>
        </form><br>
        <form action='/quotes' method='get'>
            <input type='submit' name='skip' class="btn btn-success pull-right" value='Skip to quotes!'>
        </form>


        <!-- tip to easily see objects printed in full instead of [object Object] -->
        <!-- <%- JSON.stringify(messages) %> -->
        <!-- based on the structure of your messages, determine how to display them -->
        <% if(messages.quoteErr) { %>
            <% for (var x of messages.quoteErr) { %>
            <h3><%= x %></h3>
            <% } %>
        <% } %>
    </body>
</html>